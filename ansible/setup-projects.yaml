---
  - hosts: localhost
    gather_facts: false
    tasks:
      - name: create projects
        loop: "{{ namespaces }}"
        command: "oc new-project {{ project_name }}-{{ item }} --display-name='Rocket.Chat ({{ item }})'"